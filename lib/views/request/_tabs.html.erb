<%# Intentionally overridden to remove the request sub-nav entirely %>
<%# (Search requests / Browse by category / FOI in Action). Emitting no %>
<%# content_for(:subnav) means the #subnav bar is not rendered at all. %>
